services:
  web:
    build: .
    ports:
      - "8100:80"
    depends_on:
      - db

  db:
    image: mysql:latest
    environment:
      - MYSQL_ALLOW_EMPTY_PASSWORD=yes
    volumes:
      - ./hive.sql:/docker-entrypoint-initdb.d/hive.sql
      - data:/var/lib/mysql


volumes:
  data:
